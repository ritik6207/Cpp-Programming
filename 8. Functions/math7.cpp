#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    double a = 225, x = 2, y = 4, p, s;
    p = pow(x, y); // x raised to power y that means (x^y).
    s = sqrt(a);   // Square root of a.
    cout << p << endl;
    cout << s << endl;
    return 0;
}